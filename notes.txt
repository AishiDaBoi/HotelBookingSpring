 Customer customer = new Customer();
        customer.setFirstname("Manuel");
        customer.setLastname("Weinmayr");
        customerRepo.save(customer);

        RoomCategory roomCategory = new RoomCategory();
        roomCategory.setRoomCategory(RoomCategories.Suite);
        roomCategoryRepo.save(roomCategory);

        RoomType roomType = new RoomType();
        roomType.setRoomType(RoomTypes.Standard);
        roomTypeRepo.save(roomType);

        Room room = new Room();
        room.setRoomNumber(69);
        room.setPrice(new BigDecimal("500.40"));
        room.setRoomType(roomType);
        room.setRoomCategory(roomCategory);
        roomRepo.save(room);

        Booking booking = new Booking();
        booking.setDateOfArrival(LocalDate.of(2025, 6, 12));
        booking.setDateOfDeparture(LocalDate.of(2025, 6, 19));
        booking.setCustomer(customer);
        bookingRepo.save(booking);

        RoomBooking roomBooking = new RoomBooking();
        roomBooking.setBooking(booking);
        roomBooking.setRoom(room);
        roomBookingRepo.save(roomBooking);
